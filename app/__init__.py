# Marks this directory as a regular Python package (prevents namespace package collision
# with any other installed 'app' package that has __init__.py and would win over a
# namespace package even when this directory appears first in sys.path).
